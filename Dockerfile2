# Use the official image as a parent image.
FROM ubuntu:20.10

RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y git
RUN  git clone https://github.com/rmedeiros/featurecloud.git &&\
	 cd featurecloud &&\
	 git checkout develop
	 
VOLUME featurecloud/

